import 'package:flutter/material.dart';

class DetailsSened extends StatelessWidget {
  const DetailsSened({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
