import 'package:flutter/material.dart';

class DineView extends StatelessWidget {
  const DineView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("No"));
  }
}
